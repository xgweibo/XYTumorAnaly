'use strict';

describe('myApp.setup module', function() {

  beforeEach(module('myApp.setup'));

  describe('setup controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var setupCtrl = $controller('SetupCtrl');
      expect(setupCtrl).toBeDefined();
    }));

  });
});