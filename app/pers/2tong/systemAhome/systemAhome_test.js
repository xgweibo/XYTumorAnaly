'use strict';

describe('myApp.systemAhome module', function() {

  beforeEach(module('myApp.systemAhome'));

  describe('systemAhome controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var systemAhomeCtrl = $controller('SystemAhomeCtrl');
      expect(systemAhomeCtrl).toBeDefined();
    }));

  });
});