'use strict';

describe('myApp.content module', function() {

  beforeEach(module('myApp.content'));

  describe('setup controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var contentCtrl = $controller('ContentCtrl');
      expect(contentCtrl).toBeDefined();
    }));

  });
});