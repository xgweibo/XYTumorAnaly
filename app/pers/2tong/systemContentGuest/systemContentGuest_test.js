'use strict';

describe('myApp.systemContentGuest module', function() {

  beforeEach(module('myApp.systemContentGuest'));

  describe('systemContentGuest controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var systemContentGuestCtrl = $controller('SystemContentGuestCtrl');
      expect(systemContentGuestCtrl).toBeDefined();
    }));

  });
});